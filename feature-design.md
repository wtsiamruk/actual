# Email Parser based Sync

## Relevant existing features to reuse or that would require tweaks

- openid authorization to extend it to allow email grep for gmail provider, consider other providers
- rules (probably for regex enabled logic)
- settings (need to configure email parser as expirimental feature)

## notes

Seems that i need to use the app-email-sync app for the server, and then handle the UI client separately, a good thing i think.

